# Changelog

## 5.1.1

- Initial ApexOS port of the AirCast add-on (upstream 5.1.1).
- Legacy configuration migration now reads from the ApexOS configuration
  mount (`/apexos`).
- Builds locally from the ApexOS Debian base image.
